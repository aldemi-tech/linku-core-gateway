{
  "name": "linku-core-gateway",
  "version": "1.0.0",
  "description": "Firebase Hosting gateway for Linku microservices API routing",
  "main": "index.js",
  "scripts": {
    "dev": "firebase serve --only hosting",
    "build": "echo 'No build step required for hosting'",
    "deploy": "firebase deploy --only hosting",
    "deploy:all": "firebase deploy",
    "validate": "echo 'Validation complete'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "firebase",
    "hosting",
    "gateway",
    "microservices",
    "api",
    "linku",
    "routing"
  ],
  "author": "Aldemi Tech",
  "license": "MIT",
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
